import TopBar from "../Splashpage/TopBar/TopBar";

const Dashboard = () => {
    return (
        <div className="dashboard-container">
            < TopBar />
        </div>
    );
};

export default Dashboard;
